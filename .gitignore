# Python
backend/.venv/
__pycache__/
.pytest_cache/

# Node / build artifacts (dist is rebuilt by start.sh)
frontend/node_modules/
frontend/dist/

# Reference material kept locally, not redistributed here: the method paper
# (arXiv:1212.3406, fetch it yourself) and the old batch 4D solver kept as a
# historical reference for the multi-D work. See README.md.
docs/

# Results of our simulations go here
out/

# Claude Code's data for this project (session transcripts, subagent logs,
# project memory), kept beside the repo rather than under ~/.claude:
# ~/.claude/projects/-home-tigran-python-wignerf is a SYMLINK to this path.
# Not committed — it holds whole conversations. Only this subdirectory is
# ignored, so a project-scoped .claude/settings.json could still be tracked.
.claude/projects/
